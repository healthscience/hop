import assert from 'assert'
import liveHOP from '../src/index.js'

describe('bring the protocol to life', function () {
  it('connecting server, socket, safeflow', function () {
    let hopSF = new liveHOP()
  })
})
